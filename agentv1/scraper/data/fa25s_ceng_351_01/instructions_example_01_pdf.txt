6.2 The MIPS architecture has a register set that consists of 32-bit registers. Is it possible to design a computer architecture without a register set? If so, briefly describe the architecture, including the instruction set. What are advantages and disadvantages of this architecture over the MIPS architecture?
do everything in possible. memory yes , use memory as register set > for Iwasw need - no instructions add 0x200x300x10 & , , cons ros P - - accesses Iw/sw Operation every noncedfor slower > is memory - G instructions Fewer large thread-level instructions > - easier ↳ addressable parallelism or small is memory ech nemadder a 4GB -2 bits = 256 B instr 8 bits =